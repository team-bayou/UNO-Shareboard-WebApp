import React, { Component } from 'react';

export default class LoadingNotification extends Component {
  render() {
    return (
      <div className="uk-text-center center-page">Loading...</div>
    );
  }
}
